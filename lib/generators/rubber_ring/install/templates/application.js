// Don't require jquery here because it will clash with the on from RubberRing gem
//= require_tree .

$(function() {
  console.log('Edit me in "app/assets/application.js". Used jQuery version is ' + $.fn.jquery);
});
