# you need to copy your server's keys to destination server authorized_keys

site:
  build:
    source: public/build/.
    destination: /var/www
  htaccess:
    source: public/.htaccess
    destination: /var/www
#    exclude:
#      - .git
#      - database.yml
